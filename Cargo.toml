[package]
name = "ttds_bindings"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12.14", features = ["native-tls-vendored"] }
anyhow = "1.0.97"
url-builder = "0.1.1"

[lib]
crate-type = ["lib"]
